// import express from "express";

// const resizerMiddleware = (
//   req: express.Request,
//   res: express.Response,
//   next: express.NextFunction
// ) => {
//   try {
//     const { height, width, filename } = req.query;
//     if (filename && width && height)
//   } catch (err) {
//     res.send("Error Providng wrong parameters");
//     res.send(err);
//   }

//   next();
// };
// export default resizerMiddleware;
